# KEYBOARD CONFIGURATION FILE
## the python blocks leave emtpy lines
<% pget = salt['pillar.get'] %>\
<% defaults = dict(xkbmodel='pc105', xkblayout='us', xkbvariant='', xkboptions='', backspace='guess') %>\
# Consult the keyboard(5) manual page.
%for key in defaults:
<% pkey = 'keyboard:%s' % key %>\
${key.upper()}="${pget(pkey, defaults[key])}"
%endfor

